using Terraria.ModLoader;

namespace OriMod.Items {
  /// <summary>
  /// Base material to craft into various <see cref="AbilityMedallion"/>s.
  /// </summary>
  public sealed class AbilityMaterialItem : ModItem {
    public override bool Autoload(ref string name) => false;
    // TODO: idk, anything
  }
}
